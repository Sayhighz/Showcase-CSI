// Deprecated: common upload routes were unused and unmounted from the server.
// Keeping a no-op router to avoid accidental requires breaking runtime.
const express = require('express');
const router = express.Router();

module.exports = router;